from django.apps import AppConfig


class SiteInventoryConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.site_inventory'
    label = 'apps_site_inventory'
    verbose_name = 'Site Inventory & Equipment'
