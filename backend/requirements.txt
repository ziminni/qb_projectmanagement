# BuildPro CPMS — pinned to the same baseline as Repo #1 (POS) for compatibility.
typing_extensions==4.15.0
Django>=5.2,<5.3
djangorestframework>=3.15.0
djangorestframework-simplejwt>=5.3.0
django-cors-headers>=4.3.1
psycopg2-binary>=2.9.9
asgiref>=3.8.1

# Cross-system integration: REST calls to the POS backend.
requests>=2.32.3